import 'package:flutter/material.dart';

class RequestDetailChats extends StatelessWidget {
  const RequestDetailChats({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: const Color.fromARGB(255, 255, 255, 255),
      child: Row(children: [Expanded(child: Text("Chats woooo"))]),
    );
  }
}
